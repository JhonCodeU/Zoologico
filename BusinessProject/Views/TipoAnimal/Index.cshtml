@model IEnumerable<BusinessProject.Models.TipoAnimal>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombreTipo)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombreTipo)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.codigoTipoAnimal }) |
            @Html.ActionLink("Details", "Details", new { id=item.codigoTipoAnimal }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.codigoTipoAnimal })
        </td>
    </tr>
}

</table>
